import React from 'react';
import styles from './main.module.scss';

const AbsenceInfo = () => {
    return (
        <div className={styles.absenceInfo}>
            <label className={styles.abesenceData}>Urlop</label>
        </div>
    );
};
export default AbsenceInfo;
import styles from './main.module.scss';

const Roles = () => {
    return (
        <div className={styles.rolesContainerMain}>
            <h1>Roles</h1>
        </div>
    );
}
export default Roles;